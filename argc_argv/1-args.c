#include "main.h"
#include <stdio.h>
/**
 * main - is the main
 *@argc: arguments count
 *@argv: arguments value
 *Return: Always return 0
 */

int main(int argc, char *argv[])
{
	(void) argv;

		printf("%d\n", (argc - 1));

	return (0);
}
