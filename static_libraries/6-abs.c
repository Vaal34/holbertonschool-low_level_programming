#include "main.h"
/**
*_abs - check the sign of a number
* @n: the character to check
* Return: n if n is lower or equal than 0
*/
int _abs(int n)
{
if (n >= 0)
{
	return (n);
}
else
{
	return (-n);
}
}
