#include <stdio.h>

int main(void)
{
	int n = 0;
	int r = 0;
	int d = 0;

	for (n = 0; n <= 10; n++)   
	{
		for (r = 0; r <= d; r++)
	{
		_putchar(r + '0'); 
		_putchar(',');
		_putchar(' ');
		_putchar('\n');
	}
		putchar('\n');
	return (0);
}
