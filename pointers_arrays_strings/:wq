#include "main.h"

char *_strcat(char *dest, char *src)
{

	int i;
	int c;
	char tmp;

	for (i = 0; i != 0; i++)
	{
		dest++;
	}
	for (c = i - 1; c != 0; c++)
	{
		src++;
	}
	
	tmp = *src;
	*src = *dest;
	*dest = tmp;
	
	return (0);
}
