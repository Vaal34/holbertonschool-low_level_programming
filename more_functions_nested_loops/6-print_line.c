#include "main.h"
/**
 * print_line - print a straight line
 * @n: to check
 */
void print_line(int n)
{
	int o;
	char ch = '_';

	for (o = 0; o < n; r++)
	{
		if (n <= 0)
		{
		}
		else
		{
			_putchar(ch);
		}
	}
	_putchar('\n');
} 
