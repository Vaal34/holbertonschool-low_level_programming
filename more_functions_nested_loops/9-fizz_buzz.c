#include "main.h"
#include <stdio.h>

/**
* main - it's the entry point
* Return - 0 if is true
**/

int main(void)
{
int n;

for (n = 0; n <= 100; n++)
	{
	printf("%d ", n);
	}
	putchar('\n');
	return (0);
}
