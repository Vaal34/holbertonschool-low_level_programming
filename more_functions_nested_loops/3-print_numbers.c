#include "main.h"
/**
*mul - check the code
*@b: the character to check
*@a: the character to check
*Return: the multiply of a*b
*/

void print_numbers(void)
{
int n = '0';	
	for (n = '0'; n <= '9'; n++)
	{
	_putchar(n);
	}
	_putchar('\n');
}
